# REM 3.0 TODO

## High Priority

- [ ] **Deref Repair**: Добавить автоматическую вставку операторов разыменования (`*`) для параметров-ссылок в теле извлеченной функции.
- [ ] **Smarter Lifetime Repair**: Реализовать парсинг конкретных имен аргументов из ошибок E0106 для таргетного добавления лайфтаймов.
- [ ] **Control Flow Refinement**: Реализовать проверку того, является ли `break`/`continue` выходом за пределы выделенного фрагмента (Loop Target Resolution).
- [ ] **Trait Bound Handling**: Автоматический проброс Trait Bounds при использовании Generic-параметров из внешнего контекста.

## Features

- [ ] **Method Extraction**: Поддержка извлечения методов в блоки `impl`.
- [ ] **Visibility Control**: Автоматическое определение необходимости `pub` или `pub(crate)` для извлеченной функции.
- [ ] **Doc Comments**: Перенос или генерация базовых doc-комментариев.

## Infrastructure / DevEx

- [ ] **In-memory VFS**: Ускорение проверки за счет использования виртуальной файловой системы вместо записи на диск в `CargoCheckAdapter`.
- [ ] **Proc-macro support**: Включение и настройка proc-macro server для корректного анализа кода с макросами.
- [ ] **CI Pipeline**: Настройка логов и артефактов для GitHub Actions.

## Integration Tests

- [ ] Тесты на извлечение из асинхронных блоков.
- [ ] Тесты на извлечение с глубокой вложенностью лайфтаймов.
- [ ] Тесты на некорректный синтаксис (Graceful failure).
